name: stock_predictor_app
description: AI Stock Predictor - Cross-platform mobile app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.4.0
  google_fonts: ^6.2.1
  fl_chart: ^0.70.2
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  shimmer: ^3.0.0
  intl: ^0.20.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
